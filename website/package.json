{
  "name": "homebrew-tap-site",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "bun run generate.ts",
    "preview": "bun run generate.ts && open ../dist/index.html",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
